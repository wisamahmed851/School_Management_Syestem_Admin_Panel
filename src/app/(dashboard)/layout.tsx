// TODO: Implement dashboard shell layout (Sidebar + Header + main content area)
export default function DashboardLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <div>{children}</div>;
}
